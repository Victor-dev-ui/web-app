import React from 'react';

const Features = () => {
  return (
    <div className="container">
      <h1
        style={{
          textAlign: 'center',
          marginTop: '2rem',
          marginBottom: '1rem',
        }}
      >
        Особенности
      </h1>
      — редактирования элементов списка (всегда можно удалить элемент и создать
      новый);<br></br>
      — Добавление небольшого описания к каждому элементу);
      <hr />
    </div>
  );
};

export default Features;
